# Comma-separated list of supported chains (network names from @superfluid-finance/metadata)
# These networks will be used to filter super tokens from the tokenlist
# names as per @superfluid-finance/metadata
CHAINS=eth-mainnet,polygon-mainnet,base-mainnet

# Snapshot update interval in seconds (default: 3600 = every hour)
UPDATE_INTERVAL=3600

# Port for the API server
PORT=3000

# RPC batch size for balance queries (default: 100)
RPC_BATCH_SIZE=100

# Optional: Skip specific tokens from snapshot generation
# Format is a comma-separated list of chainId:tokenSymbol pairs
# Example: SKIP_TOKENS=100:xDAIx,137:MATICx,8453:SUPx
# SKIP_TOKENS= 